{
  "name": "npg-server",
  "productName": "npg",
  "version": "0.1.0",
  "main": "dist/main.js",
  "private": true,
  "description": "Network PlayGround",
  "scripts": {
    "build": "npm rebuild node-pty && tsc",
    "build:docker": "VERSION=`node -pe \"require('./package.json').version\"` && npm run build && docker build -t npg-server:${VERSION} .",
    "build:linux": "npm run build && electron-rebuild -f -w node-pty && electron-packager . --platform=linux --arch=x64 --prune=true --out=packages --overwrite",
    "build:mac": "npm run build && electron-rebuild -f -w node-pty && electron-packager . --platform=darwin --arch=x64 --prune=true --out=packages --overwrite",
    "docker": "VERSION=`node -pe \"require('./package.json').version\"` && docker run --name npg-server --rm -it -p 8080:8080 npg-server:${VERSION}",
    "electron": "npm run build && electron-rebuild -f -w node-pty && electron dist/main.js",
    "serve": "npm rebuild node-pty && PORT=3000 ts-node src",
    "lint": "tslint -t stylish -c tslint.json -p tsconfig.json --fix",
    "schema": "ajv -s schema/npg-project-schema -d schema/npg-project",
    "dep": "npm-check -s",
    "test": "jest --coverage --no-cache --verbose",
    "start": "NODE_ENV=production node dist"
  },
  "dependencies": {
    "dockerode": "^2.5.5",
    "execa": "^0.10.0",
    "express": "^4.16.3",
    "node-pty": "^0.7.4",
    "socket.io": "^2.1.1",
    "winston": "^2.4.2"
  },
  "devDependencies": {
    "@types/dockerode": "^2.5.4",
    "@types/execa": "^0.9.0",
    "@types/express": "^4.16.0",
    "@types/jest": "^23.0.0",
    "@types/node": "^10.3.1",
    "@types/socket.io": "^1.4.34",
    "@types/socket.io-client": "^1.4.32",
    "@types/std-mocks": "^1.0.0",
    "@types/winston": "^2.3.9",
    "ajv-cli": "^3.0.0",
    "electron": "^2.0.2",
    "electron-packager": "^12.1.0",
    "electron-rebuild": "^1.7.3",
    "jest": "^23.1.0",
    "npm-check": "^5.7.1",
    "std-mocks": "^1.0.1",
    "ts-jest": "^22.4.6",
    "ts-node": "^6.1.0",
    "tslint": "^5.10.0",
    "typescript": "^2.9.1"
  }
}
